# Concept Mockups (Historical)

Every PNG in this directory is a **Concept Mockup**, created before the interactive FTXUI product
existed. These images are retained only as design-history references. They are not screenshots and
must not be used to claim implemented artwork, waveform/spectrum data, commands, progress, or state.

The current, automatically captured application screenshot is
[`../terminal-music-player-actual.png`](../terminal-music-player-actual.png).

